
eureka:
  client:
    enabled: true
    register-with-eureka: true
    service-url:
      default-zone: http://user:password@localhost:8761/eureka

spring:
  application:
    name: discoverClient

server:
  port: 8099